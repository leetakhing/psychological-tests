# 2026-02-05

## Preferences
- When online search is needed, prioritize using the `searxng` skill for privacy-respecting metasearch.
- The `searxng` skill has been installed and configured with `uv` as the Python package manager.
